# MCP Client

A web application that provides a chat interface with an AI assistant capable of tool calling through the Model Context Protocol (MCP). Connect to MCP servers to extend the AI's capabilities with custom tools, and manage conversations with user authentication.
